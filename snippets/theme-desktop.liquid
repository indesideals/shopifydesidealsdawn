<!DOCTYPE html>
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="">
  <link rel="canonical" href="{{ canonical_url }}">
  
  <!-- Desktop-Optimized Favicon -->
  <link rel="icon" type="image/svg+xml" href="{{ 'favicon.svg' | asset_url }}">

  <link rel="manifest" href="{{ 'site.webmanifest' | asset_url }}">
  
  <title>
    {{ page_title }}
    {%- if current_tags %} &ndash; tagged "{{ current_tags | join: ', ' }}"{% endif -%}
    {%- if current_page != 1 %} &ndash; Page {{ current_page }}{% endif -%}
    {%- unless page_title contains shop.name %} &ndash; {{ shop.name }}{% endunless -%}
  </title>

  {% if page_description %}
    <meta name="description" content="{{ page_description | escape }}">
  {% endif %}

  {{ content_for_header }}

  <!-- Desktop Optimized Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">

  <!-- Shared Base CSS -->
  {{ 'base.css' | asset_url | stylesheet_tag }}
  
  <!-- DESKTOP-ONLY CSS -->
  {{ 'desktop.css' | asset_url | stylesheet_tag }}
  
  <!-- DESKTOP-ONLY JavaScript -->
  {{ 'desktop.js' | asset_url | script_tag }}
</head>

<body class="desktop-experience" data-theme-type="desktop">
  <!-- Desktop Top Banner -->
  <div class="desktop-top-banner">
    <div class="desktop-banner-marquee">
      <div class="desktop-marquee-content">
        <span>🚚 FREE SHIPPING ABOVE ₹299</span>
        <span>💳 CASH ON DELIVERY AVAILABLE</span>
        <span>🔥 40% OFF SITE WIDE</span>
        <span>✨ PREMIUM QUALITY GUARANTEED</span>
        <span>📞 24/7 CUSTOMER SUPPORT</span>
        <span>🎁 FREE GIFT ON ORDERS ₹999+</span>
      </div>
    </div>
  </div>

  <!-- Desktop Header -->
  <header class="desktop-header">
    <div class="desktop-header-content">
      <!-- Left Section: Menu -->
      <div class="desktop-header-left">
        <button class="desktop-menu-toggle" onclick="toggleDesktopMenu()" aria-label="Menu">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="3" y1="12" x2="21" y2="12"/>
            <line x1="3" y1="6" x2="21" y2="6"/>
            <line x1="3" y1="18" x2="21" y2="18"/>
          </svg>
        </button>
        
        <nav class="desktop-main-nav">
          <a href="/" class="desktop-nav-link">Home</a>
          <a href="/collections/all" class="desktop-nav-link">All Products</a>
          <a href="/pages/about-us" class="desktop-nav-link">About Us</a>
          <a href="/pages/contact" class="desktop-nav-link">Contact</a>
        </nav>
      </div>
      
      <!-- Center: Logo -->
      <div class="desktop-header-center">
        <a href="/" class="desktop-logo">
          {% if settings.logo %}
            <img src="{{ settings.logo | image_url: width: 150 }}" alt="{{ shop.name }}" loading="eager">
          {% else %}
            <span class="desktop-logo-text">{{ shop.name }}</span>
          {% endif %}
        </a>
      </div>
      
      <!-- Right Section: Search & Actions -->
      <div class="desktop-header-right">
        <!-- Desktop Search -->
        <div class="desktop-search-container">
          <div class="desktop-search-box" id="desktopSearchBox">
            <input type="text" placeholder="Search for products..." class="desktop-search-input" id="desktopSearchInput">
            <button class="desktop-search-submit" onclick="performDesktopSearch()">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="11" cy="11" r="8"/>
                <path d="M21 21L16.65 16.65"/>
              </svg>
            </button>
          </div>
          <div class="desktop-search-suggestions" id="desktopSearchSuggestions"></div>
        </div>
        
        <!-- Desktop Actions -->
        <div class="desktop-header-actions">
          <a href="/account" class="desktop-action-btn desktop-account-btn" title="{% if customer %}Account{% else %}Login{% endif %}">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
              <circle cx="12" cy="7" r="4"/>
            </svg>
            <span class="desktop-action-text">{% if customer %}Account{% else %}Login{% endif %}</span>
          </a>
          
          <a href="/pages/favorites" class="desktop-action-btn desktop-wishlist-btn" title="Wishlist">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>
            </svg>
            <span class="desktop-action-text">Wishlist</span>
            <span class="desktop-wishlist-count">0</span>
          </a>
          
          <button class="desktop-action-btn desktop-cart-btn" onclick="openDesktopCart()" title="Shopping Cart">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M9 22c.55 0 1-.45 1-1s-.45-1-1-1-1 .45-1 1 .45 1 1 1z"/>
              <path d="M20 22c.55 0 1-.45 1-1s-.45-1-1-1-1 .45-1 1 .45 1 1 1z"/>
              <path d="M1 1h4l2.68 13.39c.09.46.46.78.92.78h9.4c.46 0 .83-.32.92-.78L23 6H6"/>
            </svg>
            <span class="desktop-action-text">Cart</span>
            <span class="desktop-cart-count">0</span>
          </button>
        </div>
      </div>
    </div>
  </header>

  <!-- Desktop Side Menu Overlay -->
  <div class="desktop-side-menu" id="desktopSideMenu">
    <div class="desktop-menu-overlay" onclick="closeDesktopMenu()"></div>
    <div class="desktop-menu-panel">
      <div class="desktop-menu-header">
        <h3>Navigation</h3>
        <button class="desktop-menu-close" onclick="closeDesktopMenu()">×</button>
      </div>
      <nav class="desktop-menu-nav">
        <a href="/" class="desktop-menu-link">🏠 Home</a>
        <a href="/collections/all" class="desktop-menu-link">🛍️ All Products</a>
        <a href="/pages/about-us" class="desktop-menu-link">ℹ️ About Us</a>
        <a href="/pages/contact" class="desktop-menu-link">📞 Contact</a>
        <a href="/pages/favorites" class="desktop-menu-link">💖 Wishlist</a>
        <a href="/pages/shipping-and-returns-policy" class="desktop-menu-link">🚚 Shipping & Returns</a>
        <a href="/account" class="desktop-menu-link">👤 {% if customer %}My Account{% else %}Login{% endif %}</a>
      </nav>
    </div>
  </div>

  <!-- Desktop Main Content -->
  <main class="desktop-main">
    {{ content_for_layout }}
  </main>

  <!-- Desktop Footer -->
  {% section 'footer' %}
</body>
</html>
